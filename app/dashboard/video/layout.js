export const metadata = {
  title: "Video Studio | ThinkSuite AI",
  description: "Create professional AI videos from text, images, or scripts — powered by Veo 2 and AI avatars.",
};

export default function VideoLayout({ children }) {
  return children;
}
