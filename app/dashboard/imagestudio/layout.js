export const metadata = {
  title: "Image Studio | ThinkSuite AI",
  description: "Generate AI images, remove backgrounds, bulk create marketing visuals — powered by DALL-E 3, GPT Image 1 & Gemini.",
};

export default function ImageStudioLayout({ children }) {
  return children;
}
